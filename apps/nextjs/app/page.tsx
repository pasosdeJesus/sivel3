import { LandingPage } from '@/components/LandingPage';

// Página raíz (/) - usa el componente LandingPage con locale por defecto 'en'
export default function RootPage() {
  return <LandingPage locale="en" />;
}