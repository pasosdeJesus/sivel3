#!/bin/sh

. ../.env

network=$1
if (test "$network" = "") then {
  echo "Missing network baseSepolia/celoSepolia as first parameter"
  exit 1;
} fi;
node_modules/.bin/hardhat run scripts/deployPdJCredentials.ts --network $network
