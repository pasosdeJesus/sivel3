#!/bin/sh

. ../.env

network=$1
if (test "$network" = "") then {
  echo "Missing network baseSepolia/celoSepolia as first parameter"
  exit 1;
} fi;

#if (test "$network" = "baseSepolia") then {
#  network="base-sepolia" # Lo estandar para hardhat 2 pero direccion quedara en deployments/baseSepolia como es estándar para viem
#} fi;
node_modules/.bin/hardhat run scripts/deployPdJCredentials.ts --network $network
