# R-#54: Test contracts on OpenBSD using m/edr (@ethereumjs/vm)

## Description

`bin/m contract:test` is an EDR replacement for OpenBSD/adJ that uses `@ethereumjs/vm` (pure JavaScript) instead of `@nomicfoundation/edr` (Rust/napi-rs). Napi-rs has no OpenBSD support. This enables running Hardhat-compatible contract tests on OpenBSD.

Currently, sivel3 has 3 Hardhat test suites that **cannot run** on OpenBSD due to missing EDR binary:

| Test file | Tests |
|-----------|-------|
| `SIVeL3PreAlertMarket.test.ts` | 26 |
| `SIVeL3RewardEscrow.test.ts` | 16 |
| `PasosDeJesusCredentials.test.ts` | ? |

## Tasks

### 1. Adapt test imports

Tests must change from native Hardhat imports to `contract:test`-compatible format:

```typescript
// ❌ Native Hardhat (does NOT work on OpenBSD)
import { ethers } from "hardhat"

// ✅ contract:test compatible
const { ethers } = (globalThis as any).hre
```

### 2. Run tests with contract:test

```bash
cd apps/hardhat
bin/m contract:test
```

### 3. Report missing EDR features

As tests run, features not yet implemented in `contract:test` will produce errors like `"not implemented on OpenBSD: <feature>"`. Report these as they surface so they can be added to `m/edr`.

Common missing features may include:
- `console.log` in Solidity (source-mapped stack traces)
- Coverage instrumentation (`hardhat coverage`)
- Gas reporting (`hardhat test --gas`)
- Mainnet forking edge cases

### 4. Implement missing features in m/edr (iterative)

For each missing feature surfaced by the tests:

1. Identify which EDR API call Hardhat is making
2. Implement the equivalent using `@ethereumjs/vm` APIs
3. Add fallback or graceful degradation if the feature is complex
4. Re-run tests to confirm fix

---

## Acceptance Criteria

- [x] All 3 test files use `contract:test`-compatible imports
- [x] `bin/m contract:test` runs successfully on OpenBSD
- [ ] ~82/82 tests pass — **57/82 (69%), 25 fallos requieren fixes en m/edr**
- [x] Missing features documented or implemented iteratively — ver REQ/15 en m

---

## Dependencies

- Requires `@pasosdejesus/m` with `contract:test` command
- Requires `@ethereumjs/vm` v10 (now a dependency of m)
- **pnpm override required** — wagmi pulls `@ethereumjs/tx@4` → `util@8`, `common@3`. See fix below.

### pnpm override fix

Add to `apps/nextjs/package.json`:

```json
"pnpm": {
  "overrides": {
    ...existing overrides...,
    "@ethereumjs/common": "^10.1.2",
    "@ethereumjs/util": "^10.1.2",
    "@ethereumjs/tx": "^10.1.2"
  }
}
```

Then `pnpm install`.

---

## Progress

### 2026-07-02 — Phase 3 completada en m

**Entregables en `@pasosdejesus/m`:**
1. `hre-mock.ts` — Hardhat Runtime Environment con ethers + viem
2. Mocha programático con hre inyectado vía `globalThis.hre`
3. `waitForTransactionReceipt` añadido a `EdrProvider`
4. `ContractArtifact` exportado desde `contract-test/index.ts`
5. `doc/edr.md` creado
6. 23/23 tests de `contract-test` pasando

### 2026-07-03 — Debugging en OpenBSD

Errores resueltos: Common v3/v10 (bridge CJS), `'eips' in undefined` (bloques no duplicados), `ethereumjs-bridge.cjs` not found (build incompleto). Resultado: las 4 suites ya ejecutan, pero 6 fallos `VM Exception: invalid opcode` al desplegar contratos OZ.

### 2026-07-04 → 2026-07-10 — Bugs de provider (REQ/15 en m)

Bugs resueltos en `@pasosdejesus/m`: `getAddress()`, ABI defaults, Chai polyfills, revert reason decoding, address case, BigInt comparisons, `eth_call` return `0x`. Ver REQ/15 en m.

**El bug VM CALLDATALOAD+SSTORE se investigó y documentó** — la solución es Remix-style VM config (`MerkleStateManager` + `createBlockchain` + `createEVM` + `runBlock` + `activatePrecompiles`) verificada en script standalone `scripts/repro-call-dataload-sstore-remix-provider.js`. Sin embargo, **esta solución nunca se integró en `edr-provider.ts`**.

### 2026-07-11 — Verificación con m v0.11.0 + v0.12.0

**Sivel3-side:**
- [x] Imports adaptados a `(globalThis as any).hre` en los 3 test files
- [x] pnpm override para @ethereumjs/* v10
- [x] `pnpm add -D @ethereumjs/blockchain @ethereumjs/mpt @ethereumjs/block @ethereumjs/statemanager @ethereumjs/evm`
- [x] 6 assertions corregidas: address case (2), `Number(receipt.status)` (3), BigInt `.gt()` → comparación directa (1)

**m:**
- [x] `@pasosdejesus/m` v0.12.0 publicado e instalado (commit 9b0796b)
- [ ] Remix-style VM config **no integrado** en `edr-provider.ts`
- [ ] Struct decoding **no implementado** en `readFunction` de hre-mock
- [ ] ABI default args con sobrecarga **no corregido**

**Resultado:** `bin/m contract:test` → **57/82 passing (69%)** (idéntico con v0.11.0 y v0.12.0)

**v0.12.0 solo actualizó:** CHANGELOG, doc/edr.md, doc/edr-debugging.md, REQ/15.md, comando `req` (i18n). No tocó `edr-provider.ts` ni `hre-mock.ts`.

### Desglose de los 25 fallos restantes

| # | Test Suite | Tests | Error | Causa raíz |
|---|-----------|-------|-------|------------|
| 1 | PasosDeJesusCredentials + MockUSDT | 21 | `VM Exception: revert` al desplegar contratos OZ (registerCredentialType, MockUSDT) | Remix-style VM config **no integrado** en `edr-provider.ts`. El `@ethereumjs/vm` actual falla con CALLDATALOAD en init code de constructores OZ. |
| 2 | SIVeL3PreAlertMarket | 4 | `preAlerts(id)` retorna struct con campos `undefined` | `readFunction` en hre-mock no decodifica correctamente los retornos de struct/tuple en mappings |
| 3 | PasosDeJesusCredentials | 1 | `AbiEncodingLengthMismatchError` en `mintCourseCompletion` | hre-mock `writeFunction` pasa 4 argumentos a un método que espera 2 — bug en la lógica de default args para métodos con distinta aridad |
|| **Total** | **25** | | |

### Próximos pasos (en m)

1. Integrar Remix-style VM config en `edr-provider.ts` → destraba 21 tests
2. Fix struct decoding en `readFunction` de hre-mock → destraba 4 tests
3. Fix ABI default args en `writeFunction` → destraba 1 test
4. Publicar v0.13.0
5. `pnpm update @pasosdejesus/m` en sivel3
6. Re-test → target **82/82 ✅**
