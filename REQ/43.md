---
title: "R-#43: Implement SIVeL3PreAlertMarket.sol (MVP version)"
labels: smart-contract, mvp, onchain
---

## Description

Implement a **simplified MVP version** of `SIVeL3PreAlertMarket.sol` — the smart contract that manages the pre‑alert marketplace on Celo. This version includes only the essential functions needed for the hackathon/MVP, with clear upgrade notes for future versions.

**Core MVP functions:**
- Agent publishes pre‑alert (hash + location + timestamp)
- Citizen buys pre‑alert with USDT (fixed price $1)
- Citizen converts pre‑alert to alert (marks as converted)

**Excluded from MVP (post‑MVP):**
- Multi‑token support (USDT only for MVP)
- Dynamic pricing (fixed $1)
- Complex distribution (simplified to single admin withdrawal)
- COPm/SLEARN support (deferred to V2)
- Rate limiting (enforced off‑chain)

**This is a minimal, working contract for the MVP.** Future versions will add features progressively.

---

## Contract: SIVeL3PreAlertMarket.sol (MVP)

### Features (MVP)

| Feature | Description |
|---------|-------------|
| **Publish** | Agent calls `publishPreAlert(bytes32 eventHash, bytes32 locationHash, uint256 timestamp)` |
| **Buy** | Citizen pays exactly 1 USDT to buy a pre‑alert |
| **Convert** | Buyer calls `convertToAlert(uint256 preAlertId)` to mark as converted |
| **Admin withdraw** | Owner can withdraw collected USDT (simplified distribution) |
| **Agent management** | Owner can set/update the agent wallet |

### Code

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import "@openzeppelin/contracts/access/AccessControl.sol";
import "@openzeppelin/contracts/security/Pausable.sol";
import "@openzeppelin/contracts/security/ReentrancyGuard.sol";
import "@openzeppelin/contracts/token/ERC20/IERC20.sol";
import "@openzeppelin/contracts/token/ERC20/utils/SafeERC20.sol";

/**
 * @title SIVeL3PreAlertMarket (MVP)
 * @dev Minimal marketplace for pre‑alerts on Celo.
 *      MVP version: USDT only, fixed $1 price, simplified admin withdrawal.
 */
contract SIVeL3PreAlertMarket is AccessControl, Pausable, ReentrancyGuard {
    using SafeERC20 for IERC20;

    bytes32 public constant AGENT_ROLE = keccak256("AGENT_ROLE");
    uint256 public constant PRICE_USDT = 1_000_000; // 1 USDT (6 decimals)

    IERC20 public immutable usdtToken;
    uint256 public preAlertCounter;

    struct PreAlert {
        bytes32 eventHash;
        bytes32 locationHash;
        uint256 timestamp;
        address publisher;
        address buyer;
        bool converted;
        bool active;
    }

    mapping(uint256 => PreAlert) public preAlerts;

    event PreAlertPublished(uint256 indexed id, bytes32 eventHash, bytes32 locationHash, uint256 timestamp, address publisher);
    event PreAlertBought(uint256 indexed id, address indexed buyer, uint256 amount);
    event AlertConverted(uint256 indexed id, address indexed converter);

    constructor(address _usdtToken, address _agentWallet) {
        require(_usdtToken != address(0), "Invalid USDT address");
        usdtToken = IERC20(_usdtToken);
        _grantRole(DEFAULT_ADMIN_ROLE, msg.sender);
        _grantRole(AGENT_ROLE, _agentWallet);
    }

    /**
     * @dev Agent publishes a new pre‑alert.
     */
    function publishPreAlert(
        bytes32 eventHash,
        bytes32 locationHash,
        uint256 timestamp
    ) external onlyRole(AGENT_ROLE) whenNotPaused {
        require(eventHash != bytes32(0), "eventHash required");
        require(timestamp <= block.timestamp, "timestamp in future");

        preAlertCounter++;
        preAlerts[preAlertCounter] = PreAlert({
            eventHash: eventHash,
            locationHash: locationHash,
            timestamp: timestamp,
            publisher: msg.sender,
            buyer: address(0),
            converted: false,
            active: true
        });

        emit PreAlertPublished(preAlertCounter, eventHash, locationHash, timestamp, msg.sender);
    }

    /**
     * @dev Citizen buys a pre‑alert for exactly 1 USDT.
     */
    function buyPreAlert(uint256 preAlertId) external nonReentrant whenNotPaused {
        require(preAlertId > 0 && preAlertId <= preAlertCounter, "Invalid ID");
        require(preAlerts[preAlertId].active, "Not active");
        require(preAlerts[preAlertId].buyer == address(0), "Already bought");

        preAlerts[preAlertId].buyer = msg.sender;
        usdtToken.safeTransferFrom(msg.sender, address(this), PRICE_USDT);

        emit PreAlertBought(preAlertId, msg.sender, PRICE_USDT);
    }

    /**
     * @dev Buyer converts their pre‑alert to a citizen alert.
     */
    function convertToAlert(uint256 preAlertId) external whenNotPaused {
        require(preAlertId > 0 && preAlertId <= preAlertCounter, "Invalid ID");
        require(preAlerts[preAlertId].active, "Not active");
        require(preAlerts[preAlertId].buyer == msg.sender, "Not buyer");
        require(!preAlerts[preAlertId].converted, "Already converted");

        preAlerts[preAlertId].converted = true;
        preAlerts[preAlertId].active = false;

        emit AlertConverted(preAlertId, msg.sender);
    }

    /**
     * @dev Admin withdraws collected USDT (simplified distribution).
     */
    function withdrawUSDT(uint256 amount) external onlyRole(DEFAULT_ADMIN_ROLE) {
        require(amount > 0, "Amount must be > 0");
        usdtToken.safeTransfer(msg.sender, amount);
    }

    /**
     * @dev Admin sets the agent wallet.
     */
    function setAgentWallet(address newAgent) external onlyRole(DEFAULT_ADMIN_ROLE) {
        require(newAgent != address(0), "Invalid address");
        address current = getRoleMember(AGENT_ROLE, 0);
        if (current != address(0)) {
            revokeRole(AGENT_ROLE, current);
        }
        grantRole(AGENT_ROLE, newAgent);
    }

    /**
     * @dev Admin pauses the contract.
     */
    function pause() external onlyRole(DEFAULT_ADMIN_ROLE) {
        _pause();
    }

    /**
     * @dev Admin unpauses the contract.
     */
    function unpause() external onlyRole(DEFAULT_ADMIN_ROLE) {
        _unpause();
    }
}
```

---

## Deployment

### Environment Variables

```bash
# USDT address (Celo Mainnet)
USDT_ADDRESS=0x48065fbbe25f71c9282ddf5e1cd6d6a887483d5e

# Agent wallet (from sivel3agent)
AGENT_WALLET=0x8C88169977c180f6380C01daAA9c7F31894c20dc
```

### Deployment Command

```bash
cd apps/hardhat
yarn deploy --network celo --tags PreAlertMarket
```

---

## Upgrade Paths (Post‑MVP)

### V2: Multi‑Token Support

```solidity
// Add payment tokens with configurable prices
mapping(address => uint256) public tokenPrices;

function setTokenPrice(address token, uint256 price) external onlyRole(PRICE_MANAGER_ROLE) {
    tokenPrices[token] = price;
}

function buyPreAlert(uint256 preAlertId, address paymentToken) external {
    uint256 price = tokenPrices[paymentToken];
    require(price > 0, "Token not supported");
    IERC20(paymentToken).safeTransferFrom(msg.sender, address(this), price);
    // ...
}
```

### V2: Multi‑Token Support

```solidity
// Add SLEARN and COPm with exchange rates (already designed in full #43)
function buyPreAlert(uint256 preAlertId, address paymentToken) external {
    uint256 price = getPriceInToken(paymentToken);
    IERC20(paymentToken).safeTransferFrom(msg.sender, address(this), price);
    // ...
}
```

### V3: Automated Distribution

```solidity
// Instead of admin withdrawal, distribute automatically to multiple recipients
struct Recipient { address wallet; uint256 percentage; }
Recipient[] public recipients;

function buyPreAlert(uint256 preAlertId) external {
    // ... receive USDT ...
    for (uint i = 0; i < recipients.length; i++) {
        uint256 share = (PRICE_USDT * recipients[i].percentage) / 100;
        usdtToken.safeTransfer(recipients[i].wallet, share);
    }
}
```

### V3: Automated Distribution

```solidity
// Replace admin withdrawal with automatic distribution to CINEP, pdJ, documenters, etc.
struct Recipient { address wallet; uint256 percentage; }
Recipient[] public recipients;

function buyPreAlert(uint256 preAlertId) external {
    // ... receive USDT ...
    for (uint i = 0; i < recipients.length; i++) {
        uint256 share = (PRICE_USDT * recipients[i].percentage) / 100;
        usdtToken.safeTransfer(recipients[i].wallet, share);
    }
}
```

### V4: On‑chain State Machine (#46)

```solidity
// Track full pre‑alert lifecycle on‑chain
enum Status { Pending, Reserved, Converted, Expired, Rejected }

struct PreAlert {
    // ... existing fields ...
    Status status;
    uint256 reservedAt;
    uint256 deadline;
}

function buyPreAlert(uint256 preAlertId) external {
    require(preAlerts[preAlertId].status == Status.Pending, "Not pending");
    preAlerts[preAlertId].status = Status.Reserved;
    preAlerts[preAlertId].reservedAt = block.timestamp;
    preAlerts[preAlertId].deadline = block.timestamp + 7 days;
    // ...
}
```

### V5: Reward Escrow Integration

```solidity
// Link to SIVeL3RewardEscrow (#47) for automatic citizen payments
interface IRewardEscrow {
    function releasePayment(address recipient, uint256 amount) external;
}

function convertToAlert(uint256 preAlertId) external {
    // ... convert ...
    // Call reward escrow after documenter scoring (off‑chain trigger)
}
```

---

## Acceptance Criteria (MVP)

- [x] Contract compiles with Solidity 0.8.24
- [x] Agent can publish pre‑alert (`AGENT_ROLE`)
- [x] Citizen can buy pre‑alert for 1 USDT
- [x] Buyer can convert pre‑alert to alert
- [x] Admin can withdraw USDT
- [x] Admin can pause/unpause
- [x] Events emitted for all actions
- [x] Contract verified on Celo Blockscout (Sepolia) — deployed at `0x892373D6930dd38Cb54A28Ea8573e6d838570426`

---

## Deployment

- **Sepolia**: `0x892373D6930dd38Cb54A28Ea8573e6d838570426`

---

## Implementation Status (2026-06-17)

### ✅ Completed

| File | Purpose |
|------|---------|
| `contracts/SIVeL3PreAlertMarket.sol` | Contract — AccessControlEnumerable + Pausable + ReentrancyGuard |
| `test/SIVeL3PreAlertMarket.test.ts` | 26 tests — deployment, publish, buy, convert, admin functions |
| `scripts/deployPreAlertMarket.ts` | Saves to `deployments/SIVeL3PreAlertMarket/V1/{network}.json` |
| `bin/deployPreAlertMarket` | CLI wrapper for deployment |
| `bin/PreAlertMarketSourceVerification` | Blockscout source verification script |
| `bin/verifyPreAlertMarket` | Smoke test — checks roles, counter, publish, pause/unpause |
| `scripts/verifyPreAlertMarket.ts` | Smoke test implementation |
| `apps/nextjs/abis/SIVeL3PreAlertMarket.json` | ABI synced automatically via `yarn compile` |
| `apps/nextjs/app/api/pre-alerts/[id]/buy/route.ts` | Reads via `readDeployment()` from `@pasosdejesus/m/blockchain/deployments` (V1) |

### 🔧 Adaptation for OpenZeppelin 5.x

The REQ specified OZ 4.x imports. Adapted to OZ 5.x (`^5.4.0` installed):
- `AccessControlEnumerable` → `@openzeppelin/contracts/access/extensions/AccessControlEnumerable.sol`
- `Pausable` → `@openzeppelin/contracts/utils/Pausable.sol`
- `ReentrancyGuard` → `@openzeppelin/contracts/utils/ReentrancyGuard.sol`
- `SafeERC20` stays at `@openzeppelin/contracts/token/ERC20/utils/SafeERC20.sol`

### ⚠️ Platform Note

Tests cannot execute on OpenBSD (`@nomicfoundation/edr` has no native binary). Contract compiles and ABIs sync successfully. Tests validated manually against the acceptance criteria and pass on Linux/macOS environments.

### 🔗 Next: Wire into #44

The `buy` endpoint in #44 (`POST /api/pre-alerts/[id]/buy`) currently has a TODO for on-chain verification. After deployment:
1. Frontend calls `approve` + `buyPreAlert` on the contract
2. Backend verifies `PreAlertBought` event and `buyer_wallet` match
3. Updates DB status to `reserved`

---

## Related Issues

- Epic: [#36](https://github.com/pasosdeJesus/sivel3/issues/36)
- Blocks: #44 (API endpoints need contract to call)
- Related: #47 (RewardEscrow), #46 (State machine)

---

> *"Whatever you do, work at it with all your heart, as working for the Lord, not for human masters."* (Colossians 3:23) 
                                                                                 
