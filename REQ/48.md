---
title: "R-#48: MVP frontend — pre-alert map layer, purchase, conversion, and
documenter panel"
labels: frontend, ui, mvp
---

## Description

Implement the user interface for the pre‑alert marketplace on `sivel.xyz`,
enabling citizens to:

1. **View pre‑alerts** on the map (special 🤖 markers, visible only to verified
   learn.tg users)
2. **Buy pre‑alerts** for $1 USDT (wallet connection via MiniPay/MetaMask)
3. **Convert pre‑alerts** to citizen alerts (editable JSON form)
4. **Track purchases** in a dashboard
5. **Review and score** alerts (documenter panel)

**Critical UX requirement:** Every step must inform the user about:
- The non‑guaranteed nature of rewards
- Dependence on fund availability
- Documenter discretion

---

## Tasks

### 1. Map Layer

- [x] Modify `MapComponent.tsx` to fetch pre‑alerts from `GET /api/pre-alerts`
- [x] Display pre‑alerts as grey 🔎 markers (pending) and green 🔎 (reserved)
- [x] Add learn.tg verification check before showing the layer
- [x] **Mode toggle**: Casos / PreAlertas buttons with separate map layers
- [x] **Purchase gate**: only learn.tg-verified users see buy button; non-verified see info + link
- [x] Info window: title, date, location, sources, price, "Buy" button

### 2. Purchase Flow

- [x] Create `PreAlertModal.tsx` component
- [x] Wallet connection (MiniPay detection)
- [x] **Display pre‑purchase warning** (funds not guaranteed, no refunds)
- [x] Call `POST /api/pre-alerts/:id/buy` with payment token
- [x] Show confirmation: "Expires in 7 days"
- [x] Update marker color after purchase

### 3. Conversion Flow

- [x] Create `ConvertForm.tsx` component — integrated into PreAlertModal
- [x] Display pre‑alert JSON in editable form — simplified: button in modal
- [ ] Validate against `relato.schema.json` (using `ajv` on frontend) — deferred to #50
- [x] **Display conversion warning** (reward depends on funds + quality)
- [x] Call `POST /api/pre-alerts/convert`
- [x] Show confirmation: "Awaiting documenter review"

### 4. Citizen Dashboard

- [x] Create `/dashboard` page
- [x] List purchased pre‑alerts (reserved, converted, paid, pending_reward)
- [x] Show status and deadlines
- [x] **Display fund status** ("Funds available: $X", "Pending funds")
- [x] "Convert" button for eligible alerts

### 5. Documenter Panel

- [x] Create `/documenter` page (requires `DOCUMENTER_ROLE`)
- [x] List converted pre‑alerts (from `GET /api/pre-alerts/queue`)
- [x] Score modal (0-5)
- [x] **Display available funds** before scoring
- [x] Call `POST /api/pre-alerts/:id/score`

---

## Acceptance Criteria

- [x] Pre‑alert markers appear on map (only for verified users)
- [x] Citizens can buy pre‑alerts with wallet
- [x] **Pre‑purchase warning** displayed before payment
- [x] **Conversion warning** displayed before submission
- [x] Dashboard shows purchase history and fund status
- [x] Documenters can see queue and score alerts
- [x] All API calls are authenticated (wallet signature or SBT) — ✅ verify endpoint + on-chain tx signing wired

---

## Implementation Status (2026-06-19)

### ✅ Completed

| File | Purpose |
|------|---------|
| `hooks/usePreAlerts.ts` | Hook — fetch list, detail, convert |
| `components/PreAlertModal.tsx` | Modal — purchase + conversion flow with warnings, i18n (en/es) |
| `components/MapComponent.tsx` | Added 🤖 pre‑alert markers (grey=pending, green=reserved), gated by isVerified |
| `components/OSMMapDesktop.tsx` | Passes preAlerts + isVerified + onPreAlertClick to MapComponent |
| `components/OSMMapMobile.tsx` | Same as desktop |
| `app/[locale]/cases/osmmap/page.tsx` | Wired usePreAlerts + PreAlertModal + verify + on-chain purchase |
| `app/[locale]/dashboard/page.tsx` | Citizen dashboard — shows purchased pre-alerts with status |
| `app/[locale]/documenter/page.tsx` | Documenter panel — queue list + scoring UI (2-5 or reject) |
| `app/api/verify/route.ts` | Proxies learn.tg verification — used to gate pre‑alert layer |
| `lib/buyPreAlert.ts` | Client‑side purchase — sends USDT transfer with preAlertId in calldata, no approve needed, MiniPay+MetaMask |
| `lib/contractAddresses.ts` | Hardcoded addresses for client‑side — PreAlertMarket, RewardEscrow, Credentials, SLEARN, USDT, RegionalDonation |
| `app/api/pre-alerts/[id]/buy/route.ts` | Backend verifies on‑chain Transfer event (USDT → PreAlertMarket) before recording |

### 🔗 Pre‑Alert Flow (end‑to‑end)

```
Map loads → /api/verify?wallet=... → isVerified=true
  → 🤖 markers appear
  → Click marker → PreAlertModal (warnings: no refunds, funds not guaranteed)
  → Click "Buy" → wallet signs buyPreAlert tx → sends tx_hash to backend
  → Backend verifies on‑chain PreAlertBought event → DB status: reserved
  → Citizen converts → POST /api/pre-alerts/convert → DB status: converted
  → Documenter scores → POST /api/pre-alerts/:id/score → releasePayment on-chain
  → Dashboard shows paid status
```

### 🎨 UX Features

- **Pre‑purchase warnings**: "No refunds" + "Rewards depend on available funds"
- **Post‑purchase**: Shows deadline, "Convert to Alert" button
- **Dashboard**: Status badges (reserved/converted/pending_reward/paid), deadlines
- **Documenter**: Star rating (2-5) + reject (0), feedback field

### 🔧 Architecture Notes

- Map uses lazily-geocoded department coordinates for pre-alert placement
- PreAlertModal shared between purchase and post‑purchase views
- Both desktop and mobile map layouts receive pre‑alert props

### 🔧 Changes (2026-06-22)

- Pre-alert icons: 🔎 replacing 🤖, with anti-overlap spiral offset for same-municipio alerts
- Mode toggle: Casos / PreAlertas buttons on map with separate layer groups
- Purchase gate: blue info box + learn.tg link for non-verified users
- Documenter scoring: EIP-191 `verifyMessage()` signature required + feedback mandatory for all scores
- Verify endpoint: fixed to use `signMessage(keccak256(encodePacked(...)))` matching learn.tg's `hashMessage()`
- Mainnet addresses configured in `lib/contractAddresses.ts`
- Debug Console: `bg-black/95` fix applied in `@pasosdejesus/m`

### ⏳ Remaining (2026-06-22)

| Task | Status |
|------|--------|
| Documenter panel: EIP-191 signature on score submission | ⏳ Required by updated `/api/pre-alerts/:id/score` endpoint |
| Editable JSON form for conversion | ⏳ Deferred to #50 |

---

## Dependencies

- Requires #44 (API endpoints) — ✅ API implemented
- Requires learn.tg verification (`/api/verify`) — ✅ EIP-191 signing fixed, operational
- Requires #47 (RewardEscrow contract for fund balance display) — ✅ contract deployed

- Deferred: editable JSON form → #50

---

> *"Whatever you do, work at it with all your heart, as working for the Lord,
> not for human masters."* (Colossians 3:23)
